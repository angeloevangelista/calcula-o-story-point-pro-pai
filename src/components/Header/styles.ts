import styled from "styled-components";

export const Container = styled.header`
  position: absolute;

  width: 100%;

  padding: 1.2rem;
  height: 6rem;

  background: linear-gradient(
    to bottom,
    #100e10 10%,
    #100e10c0 80%,
    transparent 100%
  );
`;
